{
  "name": "reto-gojom",
  "version": "0.0.1",
  "scripts": {
    "preinstall": "npm list local-web-server@5.2.1 -g || npm install -g local-web-server@5.2.1 && npm list sass -g || npm install -g sass",
    "start": "ws"
  },
  "dependencies": {
    "local-web-server": "^5.2.1",
    "sass": "^1.55.0"
  },
  "globalDependencies": [
    "local-web-server"
  ]

}
